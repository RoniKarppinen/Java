public interface Payment {
    /*
     * Abstract. The method calculates and return returns the salary as double (no
     * functionality here).
     */
    public abstract double calculatePayment();
}
